# Problem
This is a table test for *DPK08 - Map*:

**Create a function that can map a function execution to each element of a list, return a new list.**
```
map([1,2,3,4,5], (x) => x * 2) -> [2,4,6,8,10]
```

**The map function should receive a collection (list or array) and a function that will be applied to each element of the collection.**

**Now refactor the code and do not use any prebuild function.**

## Table Test - Implementation 1
